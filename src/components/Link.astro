---
const { href, class: className } = Astro.props;
---

<a
	href={href}
	target="_blank"
	class={`underline decoration-accent underline-offset-8 decoration-2 hover:decoration-[3px] ${
		className ?? ""
	}`}><slot /></a
>
